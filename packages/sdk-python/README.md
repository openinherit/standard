# openinherit Python SDK

Python validation SDK for the INHERIT open estate data standard.

```python
from openinherit import validate

result = validate(document)
print(result["valid"])  # True or False
```

See [openinherit.org](https://openinherit.org) for full documentation.
